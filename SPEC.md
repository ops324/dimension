# DIMENSION — 仕様書

高次元可視化Webサイト「DIMENSION｜高次元を見る」の設計仕様。
実装済みの挙動と、その値が選ばれた**理由**(多くは実測に基づく)を記録する。

| | |
|---|---|
| 公開URL | **https://ops324.github.io/dimension/** |
| リポジトリ | https://github.com/ops324/dimension |
| 版 | **Phase 16 時点**(PR #1〜#28 マージ済み)。Phase 12b までは独立監査エージェントがコードとの整合性を検証済み。Phase 13 / 14a / 14b / 14c は独立監査のプランに基づいて実装し、記載の数値はすべてブラウザ実測。Phase 15 は解説の**読み手**を広げ(§7.6)、Phase 16 は物語を**触れる**ものにした(§7.7) |
| 形態 | 完全静的サイト(サーバー処理・API・環境変数なし) |
| 対応 | モダンブラウザ(WebGL2 必須)/ デスクトップ・モバイル・横持ち |

---

## 1. コンセプト

人間は3次元までしか知覚できない。しかし数学は、その先へ正確に進むことができる。
本サイトは「見えない構造」を、**科学的に正確でありながら芸術作品として成立する**ビジュアルとして体験させる。

設計上の優先順位(競合したときはこの順で判断する):

1. **数学的正しさ** — 投影・回転・断面は近似や演出的な嘘を含まない
2. **図の可読性** — UIも演出も、図を隠すなら本末転倒
3. **芸術性** — その上で、見惚れる絵をつくる

---

## 2. 体験の全体像

```
                          ┌──── ?gallery=<id> の深リンク ────┐
                          ↓                                  ↓
[プリローダー] → [物語モード:次元の階段] --CTA / skip link--> [ギャラリーモード:4展示]
                        ↑                                    ↓
                        └── トップナビ / Esc / ブラウザの戻る ─┘
```

モードと表示中の展示は URL に載る(§6.1)。CTA・skip link・トップナビ・Esc・
ブラウザの戻る/進む ── **入口と出口はすべて履歴という 1 本の経路を通る**ので、
URL が状態について嘘をつくことがない。

### 2.1 物語モード「次元の階段」

スクロール位置が `dimLevel ∈ [0,6]` を駆動し、**同一の6次元超立方体**が連続的に押し出される。
図はキャンバスのドラッグで**見回せる**(章の構図を保ったまま原点まわりを回る / §7.7)。

| 章 | 表示語 | dim | 内容 |
|---|---|---|---|
| 序章 | DIMENSION | 0 | 導入 |
| 第〇章 | POINT | 0 | 点(広がりのない場所) |
| 第一章 | LINE | 1 | 線(前と後ろだけの世界) |
| 第二章 | PLANE | 2 | 面(回り込めるが上がない) |
| 第三章 | SOLID | 3 | 立体(私たちの部屋) |
| 第四章 | TESSERACT | 4 | 四つ目の方向・二重回転の始動 |
| 第五章 | PENTERACT | 5 | 規則だけが先へ行く |
| 第六章 | HEXERACT | 6 | ふたつの回転が同時に |
| 終章 | BEYOND | 6 | ギャラリーへの誘い |

### 2.2 ギャラリーモード(4展示)

| 展示 | 内容 | 主なパラメータ |
|---|---|---|
| HOPF FIBRATION | S³ を満たすファイバー円束のステレオ投影 | 本数24-1200・分布3種・等傾回転・ω₁/ω₂・歳差 |
| CLIFFORD TORUS | S³ 上の平坦トーラス、二重回転で裏返る | グリッドU/V 12-64・等傾・ω₁/ω₂・歳差 |
| POLYTOPE EXPLORER | n次元 超立方体/単体/正軸体(n=3〜10、既定はテッセラクト n=4) | 族・次元n・投影法 |
| PERSPECTIVE | 次元mの観測者が次元nを見る | プリセット・m・n・モード(断面/影/X線)・回転 |

各展示には「解説」がある。Phase 15 以降は**二層**で、専門用語ゼロのコーデックス(図鑑)が
表に立ち、従来の科学解説は DEEP DIVE の中に沈んでいる(§7.6)。

---

## 3. 数学コア(`src/math/` — 純TypeScript、Three.js非依存)

Three.js に依存しない純関数群。**全て vitest による数値検証の対象**(35テスト)。

### 3.1 API

```ts
// rotation.ts — 平面(i,j)のGivens回転。角度は毎フレーム絶対時刻から再計算(ドリフトなし)
rotateBatch(src: Float64Array, dst: Float64Array, n: number, count: number, rots: PlaneRotation[]): void

// projection.ts
projectPerspective(src, n, count, dist, out: Float32Array): void  // 透視カスケード nD→3D
projectOrtho(src, n, count, out): void                            // 座標 3..n−1 を捨てる
projectStereographic(src, count, out, eps=1e-4): void             // S³→R³、(x,y,z)/(1−w)

// polytopes.ts — 全て外接半径1に正規化
makeNCube(n)      // 2^n 頂点 / n·2^(n−1) 辺(+ edgeAxis: 次元別フェード用)
makeNSimplex(n)   // n+1 頂点 / C(n+1,2) 辺
makeNOrthoplex(n) // 2n 頂点 / 2n(n−1) 辺
makeFaces2(family, n)  // 2-面の列挙(スライス用)

// slice.ts — 超平面スライス
sliceFaces(rotated, n, faces, axis, offset, out): number  // 2-面∩超平面 → 線分群
sphereSliceRadius(R, s): number

// hopf.ts — 基点(θ,φ)のファイバー: p(a) = (cos(θ/2)cos a, cos(θ/2)sin a, sin(θ/2)cos(a+φ), sin(θ/2)sin(a+φ))
hopfFiber(theta, phi, segments, out, offset?): void
hopfMap(x, y, z, w): [number, number, number]
baseLatitudeRingsWeighted(rings, targetTotal, out, θmin?, θmax?): number
baseGreatCircle(count, tilt, out): number
baseFibonacci(count, out): number
```

### 3.2 設計判断

- **絶対角の契約** — 回転角は毎フレーム `angle = ω·t` で再計算する。増分積算は浮動小数誤差でノルムがドリフトするため禁止。ただし物語シーンは重み付き積分位相を使う(重みが変わっても角度が飛ばないため)
- **外接半径1への正規化** — 透視カスケードの分母 `dist − p[d]` の安全性の根拠。ただし**カスケードの第1段しか保証しない**(下記 5.4)
- **スライスは2-面法** — 断面の辺は「元の多胞体の各2-面と超平面の交差線分」に一致する。凸包計算が一切不要。テッセラクトの2-面は24枚と軽量
- **面積重み付きリング分布** — S² 上の一様密度は緯線周長 2π·sinθ に比例する。等本数リングは小θで同一円に重なり加算合成が飽和した(実測)

### 3.3 検証(`src/tests/`、35テスト)

回転のノルム保存(1e-12)/逆回転の恒等性/投影の手計算値一致/ステレオ投影の半径則 √((1+w)/(1−w))/**Hopf写像のファイバー上不変性**/既知の頂点・辺・面数(10次元超立方体 = 1024頂点・5120辺 等)/正単体の等辺性/**既知断面**(立方体→正方形4、テッセラクト→立方体12、16-cell→正八面体12、5次元超立方体→テッセラクト32)/GPU回転行列とCPU回転の等価性(1e-9)

---

## 4. レンダリングアーキテクチャ

### 4.1 構造

```
Engine ─ WebGLRenderer(1つ)
       ├ PerspectiveCamera(1つ、全シーン共有)
       ├ EffectComposer: RenderPass → UnrealBloom → OutputPass → GradePass
       └ rAFループ(dt上限 1/20 秒)

シーン切替 = renderPass.scene の差し替えのみ(composer は再生成しない)
starfield  = 共有オブジェクトをアクティブシーンへ add() で付け替え
```

### 4.2 ゼロアロケーション契約

毎フレームのホットパスでオブジェクトを生成しない。

```
角度更新(絶対時刻) → rotateBatch → 投影 → 辺インデックスで線分バッファへ scatter
                  → commitPositions / commitColors → GPU
```

- `Float32Array` は **BufferAttribute の実体そのもの**(コピーなし)
- `needsUpdate` は **InterleavedBuffer** に立てる(attribute 側では GPU に反映されない — 定番の罠)
- `setPositions()` / `setFromPoints()` は毎回確保するため構築後は呼ばない
- 動的バッファのオブジェクトは `frustumCulled = false`

実測: 10次元超立方体(25,600細分点・10次元)のCPUホットパスは **1.74ms/frame**。

### 4.3 4D属性GPUパス(Hopf / Clifford)

S³上の4D座標を**静的な** `instanceStart4` / `instanceEnd4` 属性として焼き込み、`onBeforeCompile` で LineMaterial の頂点シェーダーを拡張:

```glsl
vec4 rs4 = uRot4 * instanceStart4;          // SO(4) 回転(uniform mat4)
vec3 s3 = rs4.xyz / max(1.0 - rs4.w, uStereoEps);  // ステレオ投影
if (dot(seg, seg) > uMaxSegLen2) { e3 = s3; }      // 極ストリークを縮退
```

毎フレームのCPUコストは4×4行列の合成1回のみ、バッファ再アップロードはゼロ。**1200ファイバー × 192セグメント(23万線分)** が実用範囲に入る。
three のシェーダー文字列が変わり置換アンカーを失った場合は、警告してCPUパスへフォールバックする。

### 4.4 線と点

- **線は全て Line2(ファットライン)**: `gl.lineWidth` はほぼ全環境で1px固定のため。加算合成・`depthWrite:false`・vertexColors
- **辺の細分割** — 透視カスケードでは高次元の直線辺が3Dでは**曲線**に投影されるのが数学的に正しい。n≦6:16分割 / n≦8:8分割 / n≧9:4分割
- **頂点** — カスタムシェーダーの Points(ガウス放射グロー)

### 4.5 ポストプロセス定数(実測で決定)

| 項目 | 値 | 根拠 |
|---|---|---|
| ブルーム閾値 | **0.28** | 0.1 では画面全体に床光が乗り、線のコントラストが0.4〜1.0まで潰れていた(実測) |
| ブルーム強度 | 0.40(HIGHは0.32) | 内部解像度とペアで決める |
| ブルーム半径 | 0.25(BALANCEDは0.18) | |
| ビネット | 0.18 | |
| フィルムグレイン | 0.015 | 0.035 では高周波エネルギーの60%を占め「フィルター」の質感になった。DPRに応じて減衰 |
| 色収差 | 0.4px | |
| ディザリング | ±0.5/255 | 暗部グラデーションのバンディング防止(高精細の要) |
| トーンマッピング | ACESFilmic / exposure 1.1 | 加算線の白飛び回避 |

**フォグは味方**(直感に反する重要な知見): 黒への指数フォグは遠方ジオメトリがブルームの床光になる前に減衰させる。密度は Hopf 0.11 / Clifford 0.10 / 物語・多胞体 0.05。星背景のマテリアルはフォグ非対応のまま(背景が消えるため)。

修正効果の実測(線コントラスト = (ピーク−床)/床):

| シーン | 修正前 | 修正後 |
|---|---|---|
| HOPF | 0.065 | **2.20** |
| CLIFFORD | 0.426 | **6.28** |
| POLYTOPE | 1.065 | **4.14** |
| 物語 第4章 | 1.113 | **5.81** |

### 4.6 品質ティア

| ティア | DPR上限 | MSAA | ブルーム解像度 | 強度 | 半径 | 密度 |
|---|---|---|---|---|---|---|
| BALANCED | 1.5 | 2 | 0.75 | 0.40 | 0.18 | 0.5 |
| HIGH(起動時) | 2 | 4 | 1.0 | 0.32 | 0.25 | 1 |
| ULTRA | 3 | 8 | 1.0 | 0.40 | 0.25 | 1 |

- **エスカレーション起動**: HIGH で起動 → 最初の60フレームの平均が **10ms未満なら ULTRA へ昇格**、**22ms超なら BALANCED へ直行**(以後は降格済み扱い)、その間なら HIGH のまま。「最大で起動して降格」は初回のカクつきと弱GPUでのコンテキストロストを招くため採らない。バックグラウンドタブで計測窓が汚れた場合は窓を破棄する
- **AUTO降格**: 60フレーム窓の平均が18ms超を2窓連続で1段降格。**セッション中1回だけ再昇格可能**(20窓連続12ms未満)— 一度の不調で画質が固定される問題への対処
- **手動固定**: 右下チップで AUTO / ULTRA / HIGH / BALANCED を選択(自動制御は停止)
- `bloomScale` を単独で動かしてはならない(内部解像度を変えると同じ強度でも見た目が変わる)

---

## 5. 展示仕様

### 5.1 HOPF FIBRATION

S³ ⊂ C² において、ホップ写像 h(z₁,z₂) = (2Re(z₁z̄₂), 2Im(z₁z̄₂), |z₁|²−|z₂|²) の逆像(ファイバー)は大円になり、互いに絡み合う(ホップリンク)。

- 既定600本(上限1200)× 192セグメント
- 分布: 緯線リング(面積重み付き)/ 大円 / フィボナッチ
- 彩色: 経度φでIQコサインパレット、輝度は `0.2 + 0.8·sinθ`(赤道が最も明るく、極寄りも消えない)。ファイバー内では一定 —「1本の光の輪」として読めることを優先
- θ範囲は極を避けてクランプ(w→1 で投影が無限へ飛ぶため)

### 5.2 CLIFFORD TORUS

{(cos u, sin u, cos v, sin v)/√2} — S³ 上の**平坦**トーラス。S³ を二つの合同なソリッドトーラスに分ける境界。

- u円族・v円族(各既定36本 × 160セグメント)を色で区別(シアン系 / マゼンタ系)
- 歳差平面は **(0,3)**。(0,2) では w が |w|≤0.707 に固定され、**裏返りが原理的に起きない**(実装時の解析で判明)。(0,3) では γ=π/4(既定 t≈15.7秒)で w=1.0000 に到達し、投影半径が81まで膨張して自身を通り抜ける

### 5.3 POLYTOPE EXPLORER

- 族3種 × n=3〜10(ギャラリー既定はテッセラクト = cube n=4。クラス単体の既定値 n=10 は性能ストレス検証用で、`?exhibit=polytope` もギャラリー既定を使う)。最悪ケース(10次元超立方体)で1024頂点・5120辺・20,480線分
- 色は**隠れた軸の深度**(回転後の最終軸座標)— 4次元以上の奥行きを色として読ませる
- 密度補正: 加算合成の重なりは辺数に比例するため、基礎輝度に `1/edgeCount` の線形補正(無補正の10次元超立方体は画面が完全な白面になった)

### 5.4 PERSPECTIVE(視点次元セレクタ)

観測者次元 m ∈ {2,3,4} × 対象次元 n ∈ {2..6}。

| モード | 条件 | 内容 |
|---|---|---|
| 断面 | m < n | 対象が観測者超平面を通過する断面のモーフ + 神視点インセット |
| 影 | m < n | n→m の投影(直交/透視) |
| X線俯瞰 | m > n | 「上の次元からは内側がすべて見える」演出シーン |

- **神視点インセット** — メインレンダラーの scissor 第2パスで描画(第2のcanvas/WebGLRendererを作らない)。マッピングは inset(X,Y,Z) ← object(x₀, x_axis, x₁) で、超平面 {x_axis = s} が文字通り水平面 Y=s になる
- **掃引振幅は対象の厚みに追従** — 外接半径基準の固定振幅では断面が45%の時間で空になった(テッセラクトの座標は±1/2で、外接半径1に届くのは対角が揃った姿勢のみ)。姿勢ごとに実測して1.04倍を使い、断面が存在する時間比率 55%→83%
- X線シーン: (m=3,n=2) 平面世界の家・部屋・住人を斜め上から / (m=4,n=3) 3D の家をX線透過(壁も皮膚も隔てにならない)

### 5.5 物語シーン「extentsモーフ」

全章が**同一の6次元超立方体**(64頂点・192辺)。

```
extent[k] = clamp01(dimLevel − k)          // k = 0..5
input[v*6+k] = base6[v*6+k] * extent[k]    // 回転・投影の前にスケール
```

dimLevel=0 で点、1で線、3.5で立方体がテッセラクトへ半分押し出し中 — **連続的・可逆・スクラブ可能**な押し出しモーフになる。

- 回転スケジュール: (0,1)(0,2) は常時、(1,2) は低次元でのみ弱く回り4次元ゲートで消える(2次元の正方形が周期的に線に潰れるのを防ぐ)、**等傾二重回転 (0,3)+(1,2) は dimLevel 3 から重み付きで始動**(4次元到達の感情的な山場)、(2,4) は4から、(0,5) は5から
- 重みは積分位相で適用(重みだけを切ると位相が凍り、巻き戻し時に形が潰れる — 実装時に発見)
- 次元kに沿う辺は輝度 `extent[k]`。伸長中の軸はゴールドへブレンド(次元が生まれる瞬間の閃光)

---

## 6. スクロール機構

- **スクロールリスナーを一切張らない**。毎フレーム rAF 内で `window.scrollY` を読む(モバイルSafariのリサイズストーム耐性)
- セクション実測は `remeasure()` に集約(生成時 / debounce後のresize / `fonts.ready` / 初回フレーム)
- 章内 localT の**前半40%で次元を伸ばし、残り60%は読書のプラトー**
- `expSmooth`(rate 6)でフレームレート非依存の平滑化
- 物語→ギャラリーはスクロールハイジャックせず、エピローグのCTAボタンで明示遷移(scrollY保存・復元)

### 6.1 URL と履歴(`src/core/route.ts` — Phase 13)

モードと表示中の展示は **`?gallery=<exhibitId>`** に載る(物語 = パラメータ無し)。
これが無かった頃は「展示を人に送れない」だけでなく、**ギャラリー入場後の戻る /
iOS の端スワイプでサイトごと離脱していた**。

| 操作 | 履歴 |
|---|---|
| 物語 → ギャラリー | 物語エントリへ `replaceState({d:'narrative', y:scrollY})` してから `pushState({d:'gallery', exhibit, owned:true})` |
| タブ切替 | `replaceState` — **4展示で履歴を汚さない**(戻るは常に「入る前」へ帰る) |
| Esc / トップナビ「物語」 | `owned` なら `history.back()`、深リンクなら `replaceState` |
| popstate | URL を再パースして `Gallery.applyRoute()` へ。**Esc と同じ 1 本の階段**を降りる |

判断の根拠:

- **ハッシュは不可** — `#gallery` も `ch-prologue`…`ch-epilogue` も実在の id。フラグメント
  スクロールは**物語の状態そのものである scrollY** を壊し、ScrollDirector の毎フレーム読みと食い合う
- **パスは不可** — GitHub Pages に SPA フォールバックが無い。`/gallery/hopf` はリロードで
  404 になり、`/dimension/` のサブパスも落ちる
- **`?exhibit=` と重ねない(必須)** — `?exhibit=` は同じ4つの id を取る単独ブートで、
  `#narrative` / `#hud` / `#gallery` / `#mode-nav` を **DOM から削除する**。共有リンクが
  そちらへ落ちたら受け取った人はクローム無しの開発ハーネスに閉じ込められる。
  `main.ts` は `?exhibit=` を**先に**判定し、そのとき Router は構築しない
- URL は必ず `new URL(window.location.href)` から組む — サブパス配信・カスタムドメイン・
  他のクエリ・ハッシュが、この層が何も知らないまま保たれる
- **`owned` は履歴エントリ自身に焼く**。進む/戻るでエントリと一緒に旅するので往復しても
  判定がずれず、リロードも越える。深リンク(エントリ0)では `back()` を**呼ばない** ──
  呼べばサイトを離れる、まさに直そうとしているバグそのものになる
- **没入モードとドロワーは履歴に入れない** — `setUiHidden` はキャンバスの素のタップから
  到達するので、作品を見回すだけで履歴が溜まる
- **遷移中の popstate は 1 つだけ預かる**(`pendingRoute`)。`enterGallery`/`exitGallery` は
  `busy` で早期 return し void を返すので、popstate 側は却下を検知できない。`busy` は幕の
  40+480+40 = **560ms** 続き、iOS の端スワイプ連打はこれより速い。落とすと**履歴インデックスが
  恒久的にずれる**。10連打の実測で最終 URL と描画モードが一致することを確認済み
- **深リンク入場は幕を張らない** — `.tx` は z-index 80、`.pl` は 90 なので、幕はプリローダの
  **下**で動く。被せても 1.1 秒ぶん「止まったローダー」が伸びるだけになる
- **`history.scrollRestoration` は `'auto'` のまま**。`ScrollDirector` の初フレーム処理
  (リロード復帰時の 0 からの助走を防ぐ)がブラウザの自動復元の上に建っている。
  実測: ギャラリーでリロード → 戻る、でブラウザは 2080 を復元したが、フェード
  コールバック内(約250ms後)の `scrollTo(0, 6800)` が勝った。**競合しない**
- 物語エントリに焼く `y` は `savedScrollY` が救えない唯一の場合のため ── ギャラリーに
  居る状態でリロードすると新しい Gallery の `savedScrollY` は 0 になる(実測で確認)

---

## 7. UI/UX アーキテクチャ

### 7.1 コンポーネント基盤(`src/ui/components/`)

依存ライブラリなし。共通契約:

```ts
interface Component { readonly el: HTMLElement; destroy(): void; }
h(tag, className?, attrs?): HTMLElement   // 全コンポーネント共通のDOMビルダー
play(el, keyframes, options)              // Web Animations API の単一入口(reduced-motionガード内蔵)
```

| コンポーネント | 役割 |
|---|---|
| Preloader | 文字が1字ずつ立ち上がる入場。進捗は実際の準備状態(フォント→初回フレーム)に連動、最低表示1.1秒、上下に割れて退出 |
| Cursor | ドット+遅延追従リング。ホバー1.45倍、キャンバス上で「DRAG」表示。タッチ端末では非搭載 |
| SplitText | 行/文字のマスクリビール(aria保持) |
| MagneticButton | カーソルへの吸着(半径・強度指定、スプリングバック) |
| TransitionOverlay | 1pxグラデーションが先導する2層ワイプ |
| Tabs | 番号+EN+JPの3段、インジケータFLIPスライド、完全なtablistキーボード操作 |
| ExhibitHeader | 章題の文字リビール(入りは劇的に、出は静かに220ms) |
| Drawer | 解説ドロワー(role=dialog、コーデックス構造をブロック単位の階段状リビール / §7.6) |
| Hud | モード連動の読み取り値(物語: x.xx D / ギャラリー: EXHIBIT n/4) |
| controls/ | Slider(ドラッグ中の値バブル)/ Segmented(ピル滑走)/ Toggle / Readout / PanelButton / Panel |
| SoundToggle / ImmersiveToggle | 音響・没入モードのチップ |
| Announcer | 状態の変化を告げる `.sr-only` ライブリージョン(**`<body>` 直下必須** / §9.1) |

- `createPanel(root, title)` ファサードを維持しており、**展示側のパネル構築コードは差分ゼロ**でコンポーネント化された
- 全コンポーネントが `prefers-reduced-motion` に対応(durations 0 / translate 無効)

### 7.2 デザイントークン

```css
--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
--ease-out-quint: cubic-bezier(0.22, 1, 0.36, 1);
--ease-inout-soft: cubic-bezier(0.65, 0, 0.35, 1);
--t-fast: 180ms; --t-med: 420ms; --t-slow: 800ms; --t-reveal: 1100ms;
--surface: rgba(10,12,24,0.55);  --hairline: rgba(255,255,255,0.08);
--s1..--s7: 4/8/16/24/40/64/104px;   /* 8px基準 */
```

配色: 背景 `#05060f` / シアン `#4fd8ff` / マゼンタ `#ff4fd8` / バイオレット `#9d6bff` / ゴールド `#ffd24f`

### 7.3 タイポグラフィ

@fontsource で自己ホスト(CDN依存なし、オフライン動作)。

- 英語ディスプレイ: **Unbounded** 600/800(letter-spacing .14em)。字数に応じて幅を満たすよう `clamp(2.2rem, calc(var(--display-fit)/(文字数×0.98)), 10rem)` で自動フィット
- 日本語本文: **Zen Kaku Gothic New** 400/500(line-height 2.1、unicode-rangeスライスで必要グリフのみ取得)
- 数値・HUD: **Space Mono**

### 7.4 モバイル方針(図が主役)

「UIが図を隠すなら本末転倒」を原則とする。

- ボトムシートは**初期状態でコラプス**(掴み手+見出しの帯のみ。高さは実測して `--sheet-h` に書き戻す。CSSのフォールバックは 3.5rem、実測例 63px)
- **没入モード** — キャンバスをタップ(タッチのみ。マウスはオービット操作と衝突するためアイチップ専用)で全UIがフェード。Esc の優先順位は **① 没入モードの復元 → ② ドロワーを閉じる → ③ ギャラリー退出**
- **セーフエリア補正(構図)** — `camera.setViewOffset` でシートやタブ分を避け、被写体が空き領域の中央に来る(実測111px上昇)
- **セーフエリア補正(クローム / Phase 14a)** — 下記 7.5
- **縦持ちドリー** — 縦画面ではFOVが縦基準で横視野が3.46倍狭くなるため、カメラを最大1.6倍引く(可視幅3.81→6.10ユニット)
- **全画面スクリムの禁止** — 文字可読性のための画面全体グラデーションはコントラストを59%奪う。テキストブロック直下の局所グラデーション(画面暗化は1/6)+2段シャドウで代替
- 横持ちの短い画面(高さ<600px)はデスクトップのサイドドック配置へフォールバック

### 7.5 セーフエリア(`env()` / Phase 14a)

`index.html` は `viewport-fit=cover` を宣言しているのに、`env()` を**一度も読んでいなかった**。
モバイルの `--hud-inset` は 16px で、iOS のホームインジケータ帯 34px の**内側**にチップが座り、
さらに `#gallery-panel { bottom: 0 }` ── パネルを開く唯一の掴み手が、システムの
スワイプアップ領域と重なっていた。

**辺ごとに 4 本の派生変数を持つ。**

```css
--sa-t/-r/-b/-l: env(safe-area-inset-*, 0px);
--hud-inset-r/-b/-l: calc(var(--hud-inset) + var(--sa-*));
--gal-top-safe:      calc(var(--gal-top)   + var(--sa-t));
--gal-gutter-l/-r:   calc(var(--gal-gutter) + var(--sa-l or r));
```

- **`--hud-inset` トークン自体に `env()` を混ぜてはならない** — このトークンは
  `left:` / `right:` としても使われ、さらに `.s-hint` の**幅の式**にも入っている。
  下辺のインセットが横方向の余白へ漏れる
- **派生変数は積み上げの「底」にだけ使う** — `calc(var(--hud-inset-b) + var(--hud-stack) * 2)`。
  段の側へ足すと段数ぶん二重に数える
- モバイルのメディアクエリは `:root` の `--hud-inset` / `--gal-gutter` を差し替えるだけでよい。
  カスタムプロパティは計算値時に解決されるので派生変数は自動的に追随する
  (**メディアクエリ内で派生変数を再宣言しないこと**)

**ボトムシートの下辺は 1 ルールしか触らない。**

`#gallery-panel > .panel` に `padding-bottom: var(--sa-b)` を足す。それだけ。
チップの柱を組む `calc(var(--sheet-h, 3.5rem) + …)` の 6 か所は**不可侵**。
`box-sizing:border-box` なので padding は `max-height:46svh` の内側に入り、
シートの占有高は変わらないまま、ガラスは物理下端まで届き、操作子だけが帯の上で止まる。

ただし `Panel.publishSheetHeight()` の**畳んだときの実測はヘッダだけ**を測る
(本体の遷移の途中の高さを拾わないため)。この余白はヘッダの矩形の外にあるので、
`.panel` の `padding-bottom` を足さないと `--sheet-h` が `--sa-b` ぶん過小報告し、
**チップがシートへ 34px 食い込む**(実測)。padding は静的な値なので、
本体の遷移とは無関係という元の性質は保たれる。

**上辺は TS を足さなくてよい** — `syncSafeArea()` は `tabsEl.getBoundingClientRect().bottom` を
実測するので、CSS でタブ帯が下がれば `camera.setViewOffset` が自動で追随する。
`engine.setSafeArea` に `env()` を持ち込むと同じ量の二重実装になりドリフトする。
`perspectiveExhibit` のシザー矩形も同じ理由で実測のまま。

インセットしないもの: `#gl`(全面)/ `#progress`(2px, aria-hidden)/ `.mq` マーキー。

実測(375×812、`--sa-*` を t59 r44 b34 l44 で注入):

| 量 | インセット 0 | 注入後 | 差 |
|---|---|---|---|
| `--sheet-h` | 63.1px | 97.1px | **+34**(二重計上なら +68) |
| 展示カウンタ / 音 / 品質 / 没入チップの下端 | — | — | 各 **+34**(柱の段差 42px は不変) |
| タブ帯 | — | — | 上 +59 / 左右 各 44 内側 |

デスクトップでは `env()` が 0 を返すため、寸法は 1px も動かない。
横持ち 667×375 でもサイドドックのタブ帯・ナビ・ヘッダ・パネルがすべてノッチを避け、
`11rem` の予約(タブ帯右端とナビの間)は保たれる。

この表が裏づけるのは**算術**(足し方が正しく、二重に数えていないこと)であって、
`env()` が返す**入力値**そのものではない ── 開発環境では `env()` をエミュレートできないため。
入力値の側は**ノッチ付き実機で作者が確認済み**(縦持ち・横持ち両方向で、計器・掴み手・
タブ帯・ヘッダ・パネルのいずれも帯やノッチに潜らない)。代替測定が算術を、実機が入力値を
受け持つという分担になっている(§14.1)。

### 7.6 解説のコーデックス構造(Phase 15)

**問題**: 作品の側は「見えないものを見せる」ことに成功していたのに、解説は
ファイバー束・平坦性・族の分類を最初の一文から要求していた。読める人には最良で、
読めない人には壁だった ── 展示の入口に、専門家であることを求めていた。

**解**: 解説を**二層**にする。片方を捨てるのではなく、順序を与える。

| 層 | 中身 | 役目 |
|---|---|---|
| やさしい層(`ExhibitCodex`) | フック → 導入 → たとえ話カード → OBSERVE → TRY THIS → DATA | 「わかる」より先に**「見たくなる」**を作る |
| 深い層(`explanation`) | 従来の科学解説そのまま(一字も削っていない) | `<details>` の DEEP DIVE。潜りたい人だけが開く |

やさしい層の文体規律(`src/ui/content.ts` の型宣言に併記):

- **一文目で必ず「えっ」を作る** — フックは疑問か逆説。「このドーナツは、どこも曲がっていない。そして時々 ── 裏返る。」
- **たとえ話は 1 展示に 1 本だけ**。混ぜない(鎖かたびら / 破れない方眼紙 / 針金細工の影絵 / 水面の世界)
- **クエストは実在するパネル操作だけを指す** — ラベル名は `panel.slider` / `panel.segmented` の文字列と一致させる。「何が見られるか」を予告するのであって、操作手順を説明するのではない
- **数字は本物を使う** — 1,200本・1,024頂点・5,120辺。概数でごまかさない(§5.3 の最悪ケースと同じ値)

**アクセント色は展示ごとに 1 色**。`gallery.ts` が `#gallery` に打つ `data-exhibit` から
CSS 変数(`--dw-accent` ほか)を継承だけで全部品へ配る ── JS からの色の再配布はしない。
色そのものは§7.2 のパレット 4 色から借り、**新しい色は 1 色も発明していない**:
hopf=シアン / clifford=マゼンタ / polytope=ゴールド / perspective=バイオレット。

**深い層を SplitText で動かさないのは従来と同じ理由** — 解説は `<em>` と `<sup>` を含み、
行分割は `textContent` で組み直すので指数の組版が壊れる(S³ が S3 になる)。
数式の正しさは動きより上位。階段は段落単位からブロック単位へ移ったが、
60ms の刻みも `fill: backwards` も変えていない。

パネル注釈(`panel.note`)4件も同じトーンへ寄せた。
「頂点シェーダー内で縮退させて消している」→「地図の端では、いつも何かがはみ出す」──
**同じ事実を、シェーダーを知らない人にも同じ精度で**渡す言い換えになっている。

### 7.7 物語を触る(Phase 16)

**問題**: ギャラリーの図は掴めるのに、物語の図は見ているだけだった。同じキャンバス・
同じ図形なのに、片方は動かせて片方は動かせない ── その境目に理由が無かった。

**解**: 物語のキャンバスにも見回しを付ける。ただし **OrbitControls は使わない**。
あちらは距離も注視点も奪うので、章ごとに設計されたカメラリグと正面からぶつかる。
するのは「章が決めた位置を**原点まわりに回す**」ことだけ:

```
(x,y,z) = 章のキーフレーム × 縦長ドリー + アイドルドリフト   ← ここまでは従来通り
     ↓  球座標へ開く
方位 += yaw、極角 = clamp(極角 + pitch, …)                    ← 読者の見回し
     ↓  閉じる(半径はそのまま)
camera.position.set(x,y,z); camera.lookAt(0,0,0)
```

半径を持ち回るので**寄り具合は章のまま 1mm も動かない**。画角もドリフトも章のもの。
trig は 1 フレーム 6 回、アロケーションはゼロ。見回していない読者(yaw = pitch = 0)には
分岐ごと素通りするので、コストは文字通り 0。

| 縛り | 値 | 理由 |
|---|---|---|
| 方位 | **無制限**(画面幅で約1.4周) | 裏から見るのは物語の邪魔にならない |
| 仰角 | 章の高さから **±0.55rad(≈31°)** | 絶対角で配ると「ある章では覗けて、ある章では覗けない」になる。真俯瞰は自由ではなく**構図の放棄** |
| 極角の安全域 | 0.22〜π−0.22 rad | 真上・真下は `lookAt` の up と縮退して絵が一瞬ひっくり返る |

**タッチでは縦を取らない。** `#gl` に `touch-action: pan-y` を敷き、縦のドラッグは
ブラウザがスクロールとして処理する ── 物語は縦に読むものであり、その操作を奪うと
「見回しにもスクロールにもならない」状態が生まれる。横だけ受けるので、
**指の左右で見回し、上下で読み進める**。マウスにはこの二択が無いので上下も見回しに使う。

**ギャラリー中は黙る。** 同じ canvas に OrbitControls が付いているので、両方が効くと
1 回のドラッグが二重に解釈される。判定は `body.mode-gallery` を**ポインタを置いた
瞬間に 1 回だけ**読む(毎フレームではない)。

あわせて計器カーソルの `DRAG` を物語側でも出す ── デスクトップでは、これが
「この図は掴める」ことを知らせる唯一の合図になる。

**スクロール誘導の向き。** 序章の `SCROLL` は、横に伸びる帯の上を光が左から右へ
走っていた。求めている動作は縦のスクロールである。指の向きと誘導の向きが直交して
いると、合図として読めても身体は動かない ── 帯を縦に倒し、光は下へ落とす
(ラベルは水平のまま。読ませる文字を寝かせる理由はない)。DOM の順も
「ラベル → 帯」へ変え、読み順がそのまま **SCROLL、この向きへ** になるようにした。

---

## 8. 音響仕様(`src/audio/` — 完全プロシージャル、音声ファイルなし)

### 8.1 構成

```
カテゴリGain(ambient/sfx/bell) → masterGain → DynamicsCompressor → destination
```

- 既定 **OFF**。右下チップでオプトイン、選択は localStorage に永続化
- AudioContext はユーザー操作後にのみ生成(自動再生規制準拠)
- タブ非表示で suspend

### 8.2 音源

| 音 | 合成 |
|---|---|
| アンビエント | 55.00 + 55.35Hz(0.35Hzのうなり=呼吸)+ 110.7Hz、lowpass 220Hz / バンドパスノイズ(LFO 0.07Hz)/ 1244Hzのシマー |
| **次元の鐘** | 基音98Hzの第(k+1)倍音: **196 / 294 / 392 / 490 / 588 / 686Hz**。倍音 ×2.76・×5.40 の非調和成分でガラスの鐘に。**次元を跨ぐ瞬間に鳴る(上りも下りも)** |
| 操作音 | ホバー(37ms)/ クリック / タブのスウィープ / スライダーのティック / ギャラリー入場のグライド |

### 8.3 バイノーラル層(脳波周波数)

```
片側: Oscillator(sine, fc ∓ beat/2) → Gain(0.05) → StereoPanner(∓1) → fade → ambient
```

- **キャリア 196Hz** — 1次元の鐘と同一音。全6つの鐘と純正比(1:1, 3:2, 2:1, 5:2, 3:1, 7:2)を成す唯一の選択。55Hzのドローンはバイノーラルのキャリアには低すぎる
- **左右対称分割** — 中心音高を196Hzに固定し、各耳の変位は±5Hz以内
- **うなり周波数 = 次元の階段**:

```
beat = 2 + (4/3) × dimLevel      [Hz]
```

| 次元 | 0 | 1 | 2 | 3 | 4 | 5 | 6 / ギャラリー |
|---|---|---|---|---|---|---|---|
| Hz | 2.00 | 3.33 | 4.67 | **6.00** | 7.33 | 8.67 | **10.00** |
| 帯 | δ | δ | θ | θ | θ | α | α |

鐘が「1次元ごとに +98Hz」であるのと**同じ算術の階段**を、音高ではなく脈拍に敷く。私たちの3次元がちょうどθ波6.00Hz、ギャラリー(長時間の静止探索 = 実際に持続曝露が起きる唯一の場所)がα波10Hz固定。

- 遷移は `setTargetAtTime(τ=0.8s)` で滑らかに(スクロールの平滑化より遅らせ、音が像に追従する)
- **この層に振幅変調(LFO)を一切かけない** — かけるとモノラル手がかりが復活し、バイノーラルではなくアイソクロニックになる

### 8.4 文言規律(厳守)

効果効能を主張しない。記述するのは**刺激の事実のみ**。

- 禁止: リラックス効果 / 集中力が高まる / 脳波を整える / 瞑想状態に導く / ヒーリング / 〜に効く
- 可: 「θ波帯のうなり」「左右の耳にわずかに違う高さ」「毎秒六回」

初回有効化時に一度だけ表示: 「ヘッドフォンを ── うなりは空気ではなく、耳の中で生まれる。」
(バイノーラルは両耳分離が物理的に必須。スピーカーではモノラルのうなりに退化するが、それは許容する)

### 8.5 実測値(OfflineAudioContext、コンプレッサー通過後)

| 項目 | 値 |
|---|---|
| 左右の周波数差 | 目標値との誤差 **0.000Hz**(2/6/10Hz で検証) |
| チャンネル分離 | 109〜126dB(要求40dB) |
| 片耳の包絡線変動 | 1.005〜1.033(=平坦、真のバイノーラル) |
| アンビエント+バイノーラル | peak 0.113 / rms 0.050 |
| 全部同時(+鐘+クリック) | peak 0.185、クリッピングなし |

---

## 9. アクセシビリティ

- `prefers-reduced-motion`: 全アニメーション時間0・グレイン静止・自動回転停止
- 完全なフォーカスリング(基本 1pxシアン offset 3px、パネル内コントロールなど一部は 2px offset 2px)、`role=tablist/tab/tabpanel` のロービングtabindex、`role=dialog aria-modal` のドロワー(フォーカス復帰 + 閉じ込め / 下記 9.4)
- 章テキストは実HTML(スクリーンリーダー・検索エンジン対応)。SplitTextは aria-label とspanの aria-hidden で原文を保持
- 音響は既定OFF・完全オプトイン。光過敏性は視覚由来のため非該当だが、アイソクロニック(強い振幅パルス)は**採用しない**方針

### 9.1 作品の名前と、状態を告げる一行(Phase 14b)

**キャンバスには `role="img"` と静的な `aria-label`** を与える。これが無いあいだ、
作品そのものが支援技術に対して存在していなかった。

名前を状態に追従させないのは意図的で、**非ライブ要素の名前の変化はどの支援技術でも
確実には読まれず、読む実装の上では騒音になる**ため。加えて毎状態で書けばフレーム経路に
DOM 書き込みが増え、ゼロアロケーション契約(§4.2)を壊す。

変わる情報は `Announcer`(`src/ui/components/Announcer.ts`)が受け持つ ──
`.sr-only` + `role="status" aria-live="polite"` の一行。

- **`<body>` 直下に置く。これは要件であって好みではない。** 候補はすべて既存のルールで
  失格する: `#hud` は `aria-hidden`(**この設定は意図した設計であり、変えない** ──
  0.01 刻みで数を読み上げても騒音にしかならず、`ImmersiveToggle` と `Gallery` が
  チップを `#hud` の外へ置いているのも同じ理由)/ `#narrative` はギャラリーで
  `visibility:hidden` / `#gallery` は物語で `hidden` / `body.ui-hidden` の集合は
  没入モードで `visibility:hidden`
- **合流窓 220ms** — 速いフリックは 9 章の境界を続けざまに跨ぐ。1 つずつ書くと
  `aria-live="polite"` が行列を作り、指を止めたあとも読み上げが続く。
  窓のあいだに届いたものは**最後の 1 つだけ**を書く(実測: 4 章の高速通過 → 1 回)
- **書き込み点は 2 か所だけ**。物語は `overlays.update()` の
  `index !== lastWordIndex` ── **既存の比較を再利用する**ので毎フレームのコストは増えない。
  ギャラリーは `Gallery.applyInfo()`(展示切替の唯一の絞り点)。どちらも状態が
  変わったフレームでしか DOM に触らない
- 読み上げ文は**コンストラクタで前計算**する。`update()` の中でテンプレートリテラルを
  組むと、それだけでフレーム経路にアロケーションが生まれる
- 文面は章の題まで(本文は読まない ── 読み取りカーソルが既に到達できる実テキストで、
  流せば物語を丸ごと二重に喋ることになる)。展示は**日本語名**で告げ、
  ライブリージョン内の英語 TTS 問題を最初から避ける
- `armed` が立つまで黙る ── その前はプリローダ自身が `role="status"` を持っており、
  2 つのライブリージョンが同時に喋る

### 9.2 ランドマークと見出し(Phase 14b)

物語とギャラリーは排他なので、**ランドマークもそこで交代する**。

- `#gallery` を `<main>` にした。以前は素の `<div>` で、ギャラリーモードでは
  `#narrative`(唯一の `<main>`)が `visibility:hidden` になるため、
  ランドマークが `<nav>` だけの画面になっていた
- `#gallery-head` に `role="group"` — `<header>` が sectioning content の子でないと
  `role="banner"`(ページ級ランドマーク)にマップされ、モードの出入りで出没する
- `#gallery-en` を `h2` → `h1` へ。以前はギャラリーモードに `h1` が 1 つも無かった
- 実測: どちらのモードでも**露出しているランドマーク `main` は 1 つ、`h1` も 1 つ**。
  視覚は 1px も変わらない(スタイルはすべて id / class 基準)

### 9.3 `lang="en"` の棚卸し(Phase 14b)

`<html lang="ja">` の配下で、日本語 TTS が `TESSERACT` / `HOPF FIBRATION` を
綴り読みしていた。`h()` は未知のキーを `setAttribute` へ素通しするのでビルダーの変更は不要。

`SplitText` は `replaceChildren` しか呼ばず `restore()` が外すのは `aria-label` だけなので、
**静的に書いた `lang` はリサイズの再分割を生き延びる**。
逆に **`SplitText` の内部で `lang` を設定してはならない**(`restore()` が漏らす)。

| 付ける | 付けない(`aria-hidden` なので無意味) |
|---|---|
| `.ch-index`(器に1つで `.ch-index-no` と `.ch-index-dim` を覆う)/ `.ch-en` / `.ch-hint-label` / `#gallery-index` / `#gallery-en` / `.gh-about-en` / `.gal-tab-en` / `.panel-kicker-text` / `#quality`(器に1つで `.q-opt`×4 と `.q-readout` を覆う) | `.ch-coord` / `.ch-caption` / `.ch-rule` / `.ch-hint-rail` / `.cta-arrow` / `.mq` / `#hud` 配下すべて / `.pl-mark` |

- **`aria-hidden` の方が正しいもの**: `.nav-en`("STORY"/"GALLERY")は `.nav-jp` の
  純粋な重複。名前が「物語」「ギャラリー」になる
- **言い換えでしか直らないもの**: `aria-label` 文字列の途中に `lang` は付けられない。
  `SoundToggle` の `'環境音 SOUND'` → `'環境音'`(可視ラベルの SOUND は装飾なので名前に要らない)
- **残余(意図的に直していない)**: ドロワー題は `` `${info.jp} / ${info.en}` `` の
  1 テキストノードで、`aria-labelledby` 経由でダイアログ名になる。半分だけ言語指定する
  方法はない。EN を落とすのは**設計変更**なので a11y の変更に混ぜない

### 9.4 たどれる順序(Phase 14c)

**キーボードで作品を通り抜けられるか**は、それまで一度も検証されていなかった。

**① 不可視のフォーカス罠**(WCAG 2.4.7 違反)。`applyHidden` は伏せた章に
`opacity: 0` と `pointer-events: none` しか当てていなかった ── どちらも
**タブ順からは外さない**。しかも opacity はアウトラインにも効くので
`.cta:focus-visible` の枠線も見えない。結果、終章の `#enter-gallery` は
「文書内で最初にフォーカスできる要素」でありながら、見えも読み上げられもしない
ボタンだった(ロード直後の Tab 一回で着地し、Enter でギャラリーへ飛ぶ)。
→ 伏せているあいだ `tabIndex = -1`。ネイティブにフォーカスできる要素だけに限る。

**② スキップリンク**。①で経路を閉じた以上、キーボード / 支援技術の利用者に残るのは
9 章 × 220svh の矢印キーだけになる ── それは非開示ではなく排除である。
`:focus-visible` でしか現れない `#skip-gallery` を `<body>` の最初の子に置く
(**タブ順は位置順なので、mountChrome から差すと末尾に付いて意味をなさない**)。
初回訪問者への非開示という判断は**視覚的な注意の経済**についてのものなので、
①と②はセットで初めて中立になる ── ラベルの無い不可視の罠が、ラベルのある
意図的な 1 つのコントロールに置き換わるだけで、**開示性は差し引きゼロ**。
`<a href="#…">` ではなく `<button>` なのは、行き先が文書内位置ではなくモード変更で、
`href` はハッシュを書いて scrollY を壊すため(§6.1 でハッシュを退けたのと同じ理由)。
押したときの経路は終章の CTA と同じ CustomEvent で、単独ブートでは削除する。

**③ 畳んだボトムシート**。折り畳みは `max-height: 0; opacity: 0; overflow: hidden` で
作られている ── `display:none` でも `visibility:hidden` でも `inert` でもないので、
**畳んだままのスライダー・セグメント・トグルが十数個そっくりタブ順に残っていた**。
→ `Panel` が `is-collapsed` の切り替えで本体を `inert` にする。
**ボトムシート版レイアウトのときだけ**であることが要件(実測で踏んだ) ──
`is-collapsed` は器に常時付いているのに折り畳みの CSS は `SHEET_LAYOUT_QUERY` の
中にしか無いので、デスクトップの側面ドックで inert にすると
**見えている操作子がキーボードから消える**。回転で入れ替わりうるので resize でも揃え直す。

**④ ドロワーの焦点閉じ込め**。`role="dialog" aria-modal="true"` を宣言しながら
Tab の制御は 1 行も無かった。押された瞬間に候補を数え直す(`setContent` が本文を
差し替え、`reveal()` が見出しを再分割するのでキャッシュは古くなる)。
**Escape はここでは扱わない** ── 順序の契約は `gallery.ts` の window リスナ 1 本が持つ。
リスナは `hide()` の中で外す(`is-open` の 0.38s 遷移に載せると、`exitGallery` が
同じ同期ブロックで `hidden` にした `display:none` のサブツリーへ跳ね返そうとする)。
兄弟を `inert` にする案は採らない(7 要素に触ることになり、遷移中の例外で
ページが恒久的に inert になる)。あわせて `.dw-body` に `tabindex="0"` ──
`overflow-y:auto` なのにキーボードでスクロールする手段が無く、これが無いと
停留点が ✕ 1 つだけになって Tab が無反応になる。
Phase 15 で候補セレクタに **`summary` を追加**した ── DEEP DIVE の見出しは
ネイティブに焦点を受けるのに、`FOCUSABLE` は明示列挙なので黙って漏れる。
漏れた停留点は「閉じ込めの内側にあるのに、閉じ込めが数えていない要素」になり、
そこで Tab が外へ抜ける。**ネイティブに焦点を持つ要素を DOM へ足したら、
このセレクタも併せて見ること**(§12 の無番号項目)。

**⑤ `aria-modal` はデスクトップで嘘だった**。`.dw-scrim` は `#gallery` 内の
z-index 4 だが `#mode-nav` は 5 で覆えておらず、`body.drawer-open #mode-nav
{ display: none }` は**モバイルのメディアクエリの中にしか無かった**。
つまり解説を開いているそばからピルがクリックでき Tab でも届き、押せば
`exitGallery()` が走る。ルールをデスクトップへも適用する。

**⑥ タブの手動アクティベーション**。矢印キーは焦点を移すだけにし、選択は
Enter / Space に委ねる。それまでは矢印 1 回ごとに 380ms のシーン再構築が走り、
4 展示を通過するだけで 4 回積み上がっていた。
**マウスとタッチの挙動は 1 ミリも変わらない**。

実測(1440×900 / 375×812):

| 検証 | 結果 |
|---|---|
| ロード直後のタブ順 | `skip-gallery` → `quality-chip`。**不可視の CTA は入らない** |
| スキップリンク | Tab 一回で出現(シアンの枠)、Enter でギャラリーへ |
| デスクトップのパネル | `inert` にならず、操作子 7 つがタブ順に居る |
| モバイル畳んだシート | `pn-*` がタブ順に**出ない**。つまみを押すと全部戻る |
| ドロワー | 末尾で Tab → 先頭、先頭で Shift+Tab → 末尾、タブバーへ抜けない |
| ドロワー(DEEP DIVE 追加後 / Phase 15) | 停留点は ✕ / `.dw-body` / `summary` の 3 つ。末尾の `summary` から Tab → ✕、Shift+Tab で `summary` へ戻り、外へ出ない |
| Esc | ドロワーが閉じ、フォーカスは `#about-toggle` へ復帰 |
| 解説中のトップナビ | デスクトップでも `display: none` |

---

## 10. パフォーマンス

| 項目 | 実測 |
|---|---|
| CPUホットパス(10次元超立方体) | 1.74 ms/frame |
| Hopf 600ファイバー(11.5万線分) | GPUパスによりCPU実質ゼロ |
| バンドル(gzip) | three 130KB + アプリJS 67KB + CSS 132KB |
| 音響 | 音声アセット0(全てランタイム合成) |

CSSが大きいのは @fontsource の日本語フォント定義(121のunicode-rangeスライス)を含むため。実際に転送されるのは使用グリフを含むスライスのみ(各7〜15KB)で、単一ファイル版(966KB)を避けた結果。

---

## 11. 開発フロー

- **1フェーズ = 1ブランチ = 1 PR**(PR #1〜#21)
- **モデル分担**: 難所(数学コア・GPUシェーダー・スライス数学)と統合判断・全PRレビューは Fable、各フェーズ実装は Opus サブエージェント。**不具合修正時は独立監査サブエージェントを立て、実測に基づくプランを作ってから実装**
- **品質ゲート**(全PR必須): `npx vitest run` 全緑 + `npm run build` 成功 + ブラウザ実機確認 + コンソールエラーゼロ
  - PR #25 まで、前半 2 つは**人間の記憶に依存した約束**でしかなかった ── ワークフローの
    トリガが `push: [main]` だけで、**PR には機械のゲートが一切かかっていなかった**。
    `pull_request` を足して機械が見るようにした(§13)。後半 2 つは引き続き人間が見る

```bash
npm install
npm run dev          # 開発サーバー(--host でLAN公開、スマホ実機確認用)
npx vitest run       # 数学コアのテスト(35件)
npm run build        # 静的ビルド → dist/
npm run preview      # 本番ビルドの確認(:4173)
```

URL パラメータ:

| パラメータ | 役割 |
|---|---|
| `?gallery=hopf\|clifford\|polytope\|perspective` | **公開仕様**。ギャラリーの指定展示へ直行する共有可能なリンク(§6.1) |
| `?exhibit=hopf\|clifford\|polytope\|perspective` | **デバッグ用**。物語もギャラリーシェルも迂回し、展示単体 + OrbitControls を起動する。シェルを DOM から削除するので**共有には使えない** |

両者は決して重ねない。`?exhibit=` が先に判定され、そのとき Router は構築されない。

DEVフック `window.__DIMENSION__` はブートパスによって形が異なる:

| 起動 | 内容 |
|---|---|
| 通常(物語) | `engine, narrative, scrollDirector, overlays, quality, router, gallery(初回入場まで null), grade(on), enterGallery(), exitGallery(), setScroll(y), renderOnce(steps), audio` |
| `?exhibit=` | `engine, exhibit, quality, grade(on), renderOnce(steps), audio` |

`enterGallery()` / `exitGallery()` は**履歴経由**なので、ヘッドレス検証も本番と同じ経路
(URL 更新・戻るの行き先)を通る。

`renderOnce(steps)` はブラウザペイン非表示で rAF が止まる環境でも合成フレームを進めて1枚描画するための検証用。

**同じ環境では CSS トランジションの時計も止まる。** ドロワーの `visibility` は
0.38s の遷移に載っているので、ペインが伏せられたまま `focus()` を投げると
**`visibility: hidden` のまま**の要素を掴もうとして黙って失敗する(Phase 15 の
フォーカス実測で一度踏んだ)。測る前に遷移を終端へ送ること ──
`el.getAnimations().forEach(a => a.finish())`。ただし無限長の効果(グレインの
ループなど)は `finish()` が例外を投げるので、`try` で包む。

---

## 12. 既知の罠(実装時に踏み抜いた地雷)

番号はソース中のコメント(「既知の罠 #n」)と対応する。

1. **ステレオ投影の w→1 爆発** — 極を通るファイバーは無限遠へ飛ぶ。シェーダー内で長すぎる線分を縮退させる
2. **透視カスケードの退化** — 外接半径1の正規化が保証するのは第1段だけ。各段の倍率が累積し、9次元以上では dist=2.4 で分母の符号が反転する(実測: 10次元で投影半径178)。姿勢を走査して実効 dist を自動で伸ばす
3. **LineMaterial.resolution** — three の `LineSegments2.onBeforeRender` が毎フレーム上書きするため、`registerLineMaterial` の防御は実質無効(linewidth が CSS px 基準になるので害はない)
4. **three ヘルパの毎フレームアロケーション** — `setPositions()` / `setFromPoints()` / ループ内 `new Vector3` は禁止。`needsUpdate` は attribute ではなく **InterleavedBuffer** に立てる(attribute側では GPU へ反映されない)
5. **モバイル Safari のリサイズストーム** — アドレスバー伸縮で resize が連発する。debounce + `visualViewport` + `svh` 単位、スクロールリスナー不使用
6. **加算合成の白飛び** — 密度補正は辺数に**線形**(√ では足りない)。基礎輝度は ACES 前提で抑える
7. **タブ非表示後の dt スパイク** — `dt = min(dt, 1/20)`
8. **WebGL コンテキストロスト** — 日本語オーバーレイでリロードを案内する
9. **スライスの退化** — 超平面が頂点をちょうど通ると交差判定が不安定。オフセットに微小 eps を加える
10. **scissor の後始末** — インセット描画後に `setScissorTest(false)` とフルviewportへ復帰しないと次フレームの composer が壊れる
11. **onBeforeCompile の脆さ** — three のシェーダー文字列が変わると置換アンカーを失う。バージョンを exact で固定し、失敗時は CPU パスへフォールバック
12. **フルDPR + 高MSAA のメモリ帯域** — エスカレーション起動で弱GPUには到達させない。バックグラウンドタブで rAF が絞られた計測窓は破棄する(捨てないと弱端末と誤認して BALANCED に固定される)

番号の付いていない、しかし高くついた発見:

- **色空間リーク** — composer の RenderPass は GL のクリアカラーを使い、OutputPass 後に残った sRGB 値が次フレームの線形バッファへ流れ込む。クリアカラーは黒に固定し、背景色はネブラ球が線形値で担う
- **AudioParam.value はグライド中に古い値を返す**(Chrome)— 検証は実グラフのタップで行う
- **全画面スクリムは図の敵** — 文字可読性のためのビューポート全面グラデーションは線のコントラストを59%奪う(§7.4)
- **`--sheet-h` は「位置」ではなく「高さ」の実測値** — 畳んだシートの実測は
  `.panel-head` だけを測る(本体の遷移の途中の高さを拾わないため)。したがって
  `.panel` に足した `padding-bottom` はその矩形の**外**にあり、加算し忘れると
  `--sheet-h` が過小報告してチップがシートへ食い込む(実測 34px / §7.5)。
  逆に、柱の `calc(var(--sheet-h) …)` 側にも同じ値を足すと二重計上になる
- **`is-collapsed` はレイアウトを意味しない** — 器には常時付いているが、折り畳みの CSS は
  `SHEET_LAYOUT_QUERY` の中にしか無い。畳んだ状態を根拠に何かを無効化するときは
  **必ずシート版レイアウトかを併せて見る** ── 見ないと、デスクトップの側面ドックで
  見えている操作子がキーボードから消える(§9.4 ③ で実際に一度そうなった)
- **`opacity: 0` と `pointer-events: none` はタブ順を変えない** — しかも opacity は
  アウトラインにも効くので、伏せた要素は「見えず・読み上げられず・フォーカスできる」
  という最悪の状態になる。伏せるときは `tabIndex = -1` まで面倒を見る(§9.4 ①)
- **スクロールする画面でキャンバスを掴めるようにするなら、`touch-action` が設計そのもの** —
  `none` にすると縦に読む物語がスクロールできなくなり、`auto` のままだと横に振っただけで
  ページが揺れる。`pan-y` は「縦はブラウザ、横はアプリ」という**分担の宣言**であって、
  単なる最適化ではない(§7.7)。JS 側も同じ線で分けること ── タッチの `dy` を
  仰角に使うと、指が縦へ動くたび構図が傾きながらページも流れる
- **明示列挙のフォーカスセレクタは、ネイティブの焦点要素を黙って落とす** — `FOCUSABLE` は
  `a[href]` / `button` / 各種入力を数え上げる文字列なので、`<summary>` のように
  **属性なしで焦点を受ける要素**は列挙に無いかぎり存在しないことになる。
  閉じ込めの内側にある未計上の停留点は、そこで Tab を外へ逃がす ──
  ダイアログへ新しい種類の要素を足したら、必ずセレクタを併せて見る(§7.6 / §9.4 ④)

---

## 13. デプロイ

**GitHub Actions → GitHub Pages** で自動公開している(`.github/workflows/deploy.yml`)。

```
push to main / pull_request
  → npm ci
  → npx vitest run       # 失敗すれば公開されない(品質ゲート)
  → npm run build
  → actions/upload-pages-artifact(dist/)
  ────────────────────────────────────── ここまでは PR でも走る
  → actions/deploy-pages                 # if: push && ref == main のときだけ
```

- **`pull_request` トリガと deploy のガードはセットである**(PR #25)。トリガだけ足すと
  PR のコードがそのまま本番へ出る。`if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
- Pages の公開元は API で `build_type=workflow` に設定済み(リポジトリ設定のブランチ指定ではなく Actions 経由)
- Node は `.nvmrc`(24)に一本化。以前は `node-version: 20` の直書きで、
  **ローカル(24)と CI が食い違っていた**うえ Node 20 は 2026-04 に EOL だった
- **アクションの追随は `.github/dependabot.yml`** (月次・`github-actions` のみ)。
  派生作 dimension-lens を立ち上げる際にこの `deploy.yml` を複製して初めて、
  4 本とも 2〜3 メジャー遅れていたことが分かった(checkout v4 対 v7 など)。
  失敗の型は「直せなかった」ではなく**「気づかなかった」**なので、仕組みで塞ぐ。
  npm は対象外 ── three の exact pin は罠#11 に基づく意図的な仕様で、自動 PR がそれと戦う
- `vite.config.ts` の `base: './'`(相対パス出力)により、`https://ops324.github.io/dimension/` のようなサブパス配信でも変更なしで動く。カスタムドメインに切り替えても同様
- 手動再実行は `workflow_dispatch` でも可能

---

## 14. 今後の候補

### 14.1 検証の到達水準(何をどこまで確かめたか)

Phase 13〜15 の主張は水準の違う証拠に支えられている。**混ぜて書かないための表**。

| 水準 | 意味 |
|---|---|
| **A 実測** | 本番ビルドで値を測った。再現手順が残っている |
| **B 機構確認** | 仕組みが正しいことを確かめた。入力値そのものは代替した |
| **C 未検証** | 実機・実支援技術でしか確かめられない |

| 主張 | 水準 | 根拠と、残っている隙間 |
|---|---|---|
| URL/履歴の全経路(§6.1) | **A** | 連打10回・リロード復帰・深リンク退場をブラウザで実測 |
| セーフエリアの**算術**(§7.5) | **A** | `--sa-*` を注入して `--sheet-h` +34px / チップ +34px を実測。二重計上が無いことを確認 |
| セーフエリアの**入力値** | **A** | **作者がノッチ付き実機で確認**(縦持ち・横持ち両方向)。下部の計器とシートの掴み手がホームインジケータの帯にかからず、タブ帯・ヘッダ・パネルがノッチ側に潜らない ── `env(safe-area-inset-*)` が実値を返し `viewport-fit=cover` が効いていることの、目に見える形での裏づけ |
| canvas が画像として名前を持つ(§9.1) | **A** | ブラウザの計算したアクセシビリティツリーに `img "高次元図形のリアルタイム可視化"` として出ることを確認 |
| スキップリンクがタブ順の先頭(§9.4) | **A** | 同ツリーで文書内の先頭。本番でも確認 |
| **二重読み上げが起きない**(§9.1) | **B** | 生成 span は `sp-line` / `sp-char` とも全数 `aria-hidden="true"`、`.sr-only` 複製は 1 つ。構造上、重複の経路が無い |
| 読み上げの**聞こえ方** | **C** | **VoiceOver 未試行**。合流窓 220ms が体感として妥当か、見出しの `aria-label` + `.sr-only` 複製を読み上げが一度で済ませるかは**支援技術ごとに割れうる**ので、音でしか判断できない |
| iOS Safari の端スワイプバック(§6.1) | **A** | **作者が実機で確認**。ギャラリーから左端スワイプで物語へ戻り、サイトを離脱しない |
| DPR3 端末の品質ティア(§4.6) | **C** | シミュレータの GPU はホスト機のものなので代理にならない |
| **品質ゲートが機械に見られている**(§11) | **A** | PR #25 で `pull_request` トリガを追加し、PR 上で `build: success` / `deploy: skipped` を実測。**それまでは C だった** ── 「全PR必須」と書いてあっても、機械は何も見ていなかった |
| **CI が Node 24 と現行アクションで通る**(§13) | **A** | PR #25 でローカル(vitest 35/35・build 成功)と CI(build/deploy とも success・警告ゼロ)の両方で実測 |
| **コーデックスが4展示すべてで組み上がる**(§7.6) | **B** | hopf(シアン)と polytope(ゴールド)をブラウザで全域実測 ── フック/カード/クエスト/DATA グリッドの描画、DEEP DIVE の開閉、S³ の上付き組版、コンソールエラーゼロ。**残る2展示は同じ 1 本のコードパスを通る**(`setContent` は id を見ずに `ExhibitCodex` を組む)ので、確かめたのは機構であって全数ではない |
| **DEEP DIVE がタブ順から漏れない**(§9.4 ④) | **A** | perspective の解説で実測。停留点は ✕ / `.dw-body` / `summary` の 3 つで、**末尾の `summary` から Tab → 先頭の ✕**、そこから Shift+Tab → `summary` へ巻き戻り、いずれもドロワーの外へ出ない |
| **見回しが章の寄りを壊さない**(§7.7) | **A** | ドラッグ前後でカメラ半径 5.049 → 5.052(差はアイドルドリフトぶん)。位置は (0.03, 0.06, 5.05) → (4.25, 0.79, −2.61) と原点まわりに回っている |
| **マウスのドラッグでページが流れない**(§7.7) | **A** | 縦 500px のドラッグで `scrollY` の変化 **0**。`pointerType` は `mouse` であることも併せて確認 |
| **仰角が章の高さ ±0.55rad で止まる**(§7.7) | **A** | 上限を越えるドラッグのあと `pitchTarget = −0.5500` ちょうど。絶対極角も 41.3° に留まり、真俯瞰へ抜けない |
| **ギャラリー中に二重駆動しない**(§7.7) | **A** | ギャラリーでドラッグしてもカメラは OrbitControls だけが動かし(移動 2.803)、物語側の `yaw` / `pitch` は 1 桁も動かない |
| **タッチでは縦を取らない**(§7.7) | **B** | 合成 `PointerEvent` で検証 ── 同じ斜めドラッグに対し touch は `yaw` のみ(`pitch` = 0.0000)、mouse は両方(`pitch` = −0.2827)。**実機の指で縦スクロールが生きることは未確認**(`touch-action: pan-y` の計算値は確認済み) |

**実機確認で 2 つが C から上がった**(セーフエリアの入力値 / 端スワイプバック)。
どちらも目で見て判別できる主張なので、作者の実機目視がそのまま証拠になる。

残る C は 2 種類しかない ── **音でしか判断できないもの**(読み上げの聞こえ方)と、
**その端末の GPU でしか判断できないもの**(DPR3 の品質ティア)。
どちらも代替手段が原理的に無い: 開発環境の DevTools は `env()` も読み上げも再現できず、
iOS シミュレータなら `env()` は本物になるが GPU はホスト機のものなので性能の代理にならない
(なお検証時点ではそのシミュレータも Xcode 未導入で使えなかった)。

**表の水準は「誰がどう確かめたか」とセットで読むこと。** A のうち、ブラウザで数値を測ったものと
実機で目視したものは、根拠の列で書き分けてある。

### 14.2 記録済みの残余

- **ドロワー題の混在言語**(§9.3) — `` `${info.jp} / ${info.en}` `` は 1 テキストノードで
  `aria-labelledby` 経由でダイアログ名になる。半分だけ言語指定する方法はない。
  EN を落とすのは**設計変更**なので a11y の変更には混ぜていない
- **タブの手動アクティベーション**(§9.4 ⑥) — 矢印キーの挙動を変えた唯一の項目。
  マウス・タッチは不変だが、好みが分かれうる箇所として記録しておく

### 14.3 機能の候補

- 展示ごとのカメラホーム微調整(縦持ちドリーは現在4展示共通の1.6倍)
- 4次元回転の直接操作(ドラッグで回転平面を選ぶ)
- 展示パラメータまで URL に載せる(§6.1 の延長。「見つけた絵」をそのまま送れる)
- カスタムドメインの検討
