# DIMENSION — 仕様書

高次元可視化Webサイト「DIMENSION｜高次元を見る」の設計仕様。
実装済みの挙動と、その値が選ばれた**理由**(多くは実測に基づく)を記録する。

| | |
|---|---|
| 公開URL | **https://ops324.github.io/dimension/** |
| リポジトリ | https://github.com/ops324/dimension |
| 版 | Phase 12b + デプロイ設定 時点(PR #1〜#16 マージ済み)。独立監査エージェントによりコードとの整合性を検証済み |
| 形態 | 完全静的サイト(サーバー処理・API・環境変数なし) |
| 対応 | モダンブラウザ(WebGL2 必須)/ デスクトップ・モバイル・横持ち |

---

## 1. コンセプト

人間は3次元までしか知覚できない。しかし数学は、その先へ正確に進むことができる。
本サイトは「見えない構造」を、**科学的に正確でありながら芸術作品として成立する**ビジュアルとして体験させる。

設計上の優先順位(競合したときはこの順で判断する):

1. **数学的正しさ** — 投影・回転・断面は近似や演出的な嘘を含まない
2. **図の可読性** — UIも演出も、図を隠すなら本末転倒
3. **芸術性** — その上で、見惚れる絵をつくる

---

## 2. 体験の全体像

```
[プリローダー] → [物語モード:次元の階段] --CTA--> [ギャラリーモード:4展示]
                        ↑                                    ↓
                        └────────── トップナビ / Esc ─────────┘
```

### 2.1 物語モード「次元の階段」

スクロール位置が `dimLevel ∈ [0,6]` を駆動し、**同一の6次元超立方体**が連続的に押し出される。

| 章 | 表示語 | dim | 内容 |
|---|---|---|---|
| 序章 | DIMENSION | 0 | 導入 |
| 第〇章 | POINT | 0 | 点(広がりのない場所) |
| 第一章 | LINE | 1 | 線(前と後ろだけの世界) |
| 第二章 | PLANE | 2 | 面(回り込めるが上がない) |
| 第三章 | SOLID | 3 | 立体(私たちの部屋) |
| 第四章 | TESSERACT | 4 | 四つ目の方向・二重回転の始動 |
| 第五章 | PENTERACT | 5 | 規則だけが先へ行く |
| 第六章 | HEXERACT | 6 | ふたつの回転が同時に |
| 終章 | BEYOND | 6 | ギャラリーへの誘い |

### 2.2 ギャラリーモード(4展示)

| 展示 | 内容 | 主なパラメータ |
|---|---|---|
| HOPF FIBRATION | S³ を満たすファイバー円束のステレオ投影 | 本数24-1200・分布3種・等傾回転・ω₁/ω₂・歳差 |
| CLIFFORD TORUS | S³ 上の平坦トーラス、二重回転で裏返る | グリッドU/V 12-64・等傾・ω₁/ω₂・歳差 |
| POLYTOPE EXPLORER | n次元 超立方体/単体/正軸体(n=3〜10、既定はテッセラクト n=4) | 族・次元n・投影法 |
| PERSPECTIVE | 次元mの観測者が次元nを見る | プリセット・m・n・モード(断面/影/X線)・回転 |

---

## 3. 数学コア(`src/math/` — 純TypeScript、Three.js非依存)

Three.js に依存しない純関数群。**全て vitest による数値検証の対象**(35テスト)。

### 3.1 API

```ts
// rotation.ts — 平面(i,j)のGivens回転。角度は毎フレーム絶対時刻から再計算(ドリフトなし)
rotateBatch(src: Float64Array, dst: Float64Array, n: number, count: number, rots: PlaneRotation[]): void

// projection.ts
projectPerspective(src, n, count, dist, out: Float32Array): void  // 透視カスケード nD→3D
projectOrtho(src, n, count, out): void                            // 座標 3..n−1 を捨てる
projectStereographic(src, count, out, eps=1e-4): void             // S³→R³、(x,y,z)/(1−w)

// polytopes.ts — 全て外接半径1に正規化
makeNCube(n)      // 2^n 頂点 / n·2^(n−1) 辺(+ edgeAxis: 次元別フェード用)
makeNSimplex(n)   // n+1 頂点 / C(n+1,2) 辺
makeNOrthoplex(n) // 2n 頂点 / 2n(n−1) 辺
makeFaces2(family, n)  // 2-面の列挙(スライス用)

// slice.ts — 超平面スライス
sliceFaces(rotated, n, faces, axis, offset, out): number  // 2-面∩超平面 → 線分群
sphereSliceRadius(R, s): number

// hopf.ts — 基点(θ,φ)のファイバー: p(a) = (cos(θ/2)cos a, cos(θ/2)sin a, sin(θ/2)cos(a+φ), sin(θ/2)sin(a+φ))
hopfFiber(theta, phi, segments, out, offset?): void
hopfMap(x, y, z, w): [number, number, number]
baseLatitudeRingsWeighted(rings, targetTotal, out, θmin?, θmax?): number
baseGreatCircle(count, tilt, out): number
baseFibonacci(count, out): number
```

### 3.2 設計判断

- **絶対角の契約** — 回転角は毎フレーム `angle = ω·t` で再計算する。増分積算は浮動小数誤差でノルムがドリフトするため禁止。ただし物語シーンは重み付き積分位相を使う(重みが変わっても角度が飛ばないため)
- **外接半径1への正規化** — 透視カスケードの分母 `dist − p[d]` の安全性の根拠。ただし**カスケードの第1段しか保証しない**(下記 5.4)
- **スライスは2-面法** — 断面の辺は「元の多胞体の各2-面と超平面の交差線分」に一致する。凸包計算が一切不要。テッセラクトの2-面は24枚と軽量
- **面積重み付きリング分布** — S² 上の一様密度は緯線周長 2π·sinθ に比例する。等本数リングは小θで同一円に重なり加算合成が飽和した(実測)

### 3.3 検証(`src/tests/`、35テスト)

回転のノルム保存(1e-12)/逆回転の恒等性/投影の手計算値一致/ステレオ投影の半径則 √((1+w)/(1−w))/**Hopf写像のファイバー上不変性**/既知の頂点・辺・面数(10次元超立方体 = 1024頂点・5120辺 等)/正単体の等辺性/**既知断面**(立方体→正方形4、テッセラクト→立方体12、16-cell→正八面体12、5次元超立方体→テッセラクト32)/GPU回転行列とCPU回転の等価性(1e-9)

---

## 4. レンダリングアーキテクチャ

### 4.1 構造

```
Engine ─ WebGLRenderer(1つ)
       ├ PerspectiveCamera(1つ、全シーン共有)
       ├ EffectComposer: RenderPass → UnrealBloom → OutputPass → GradePass
       └ rAFループ(dt上限 1/20 秒)

シーン切替 = renderPass.scene の差し替えのみ(composer は再生成しない)
starfield  = 共有オブジェクトをアクティブシーンへ add() で付け替え
```

### 4.2 ゼロアロケーション契約

毎フレームのホットパスでオブジェクトを生成しない。

```
角度更新(絶対時刻) → rotateBatch → 投影 → 辺インデックスで線分バッファへ scatter
                  → commitPositions / commitColors → GPU
```

- `Float32Array` は **BufferAttribute の実体そのもの**(コピーなし)
- `needsUpdate` は **InterleavedBuffer** に立てる(attribute 側では GPU に反映されない — 定番の罠)
- `setPositions()` / `setFromPoints()` は毎回確保するため構築後は呼ばない
- 動的バッファのオブジェクトは `frustumCulled = false`

実測: 10次元超立方体(25,600細分点・10次元)のCPUホットパスは **1.74ms/frame**。

### 4.3 4D属性GPUパス(Hopf / Clifford)

S³上の4D座標を**静的な** `instanceStart4` / `instanceEnd4` 属性として焼き込み、`onBeforeCompile` で LineMaterial の頂点シェーダーを拡張:

```glsl
vec4 rs4 = uRot4 * instanceStart4;          // SO(4) 回転(uniform mat4)
vec3 s3 = rs4.xyz / max(1.0 - rs4.w, uStereoEps);  // ステレオ投影
if (dot(seg, seg) > uMaxSegLen2) { e3 = s3; }      // 極ストリークを縮退
```

毎フレームのCPUコストは4×4行列の合成1回のみ、バッファ再アップロードはゼロ。**1200ファイバー × 192セグメント(23万線分)** が実用範囲に入る。
three のシェーダー文字列が変わり置換アンカーを失った場合は、警告してCPUパスへフォールバックする。

### 4.4 線と点

- **線は全て Line2(ファットライン)**: `gl.lineWidth` はほぼ全環境で1px固定のため。加算合成・`depthWrite:false`・vertexColors
- **辺の細分割** — 透視カスケードでは高次元の直線辺が3Dでは**曲線**に投影されるのが数学的に正しい。n≦6:16分割 / n≦8:8分割 / n≧9:4分割
- **頂点** — カスタムシェーダーの Points(ガウス放射グロー)

### 4.5 ポストプロセス定数(実測で決定)

| 項目 | 値 | 根拠 |
|---|---|---|
| ブルーム閾値 | **0.28** | 0.1 では画面全体に床光が乗り、線のコントラストが0.4〜1.0まで潰れていた(実測) |
| ブルーム強度 | 0.40(HIGHは0.32) | 内部解像度とペアで決める |
| ブルーム半径 | 0.25(BALANCEDは0.18) | |
| ビネット | 0.18 | |
| フィルムグレイン | 0.015 | 0.035 では高周波エネルギーの60%を占め「フィルター」の質感になった。DPRに応じて減衰 |
| 色収差 | 0.4px | |
| ディザリング | ±0.5/255 | 暗部グラデーションのバンディング防止(高精細の要) |
| トーンマッピング | ACESFilmic / exposure 1.1 | 加算線の白飛び回避 |

**フォグは味方**(直感に反する重要な知見): 黒への指数フォグは遠方ジオメトリがブルームの床光になる前に減衰させる。密度は Hopf 0.11 / Clifford 0.10 / 物語・多胞体 0.05。星背景のマテリアルはフォグ非対応のまま(背景が消えるため)。

修正効果の実測(線コントラスト = (ピーク−床)/床):

| シーン | 修正前 | 修正後 |
|---|---|---|
| HOPF | 0.065 | **2.20** |
| CLIFFORD | 0.426 | **6.28** |
| POLYTOPE | 1.065 | **4.14** |
| 物語 第4章 | 1.113 | **5.81** |

### 4.6 品質ティア

| ティア | DPR上限 | MSAA | ブルーム解像度 | 強度 | 半径 | 密度 |
|---|---|---|---|---|---|---|
| BALANCED | 1.5 | 2 | 0.75 | 0.40 | 0.18 | 0.5 |
| HIGH(起動時) | 2 | 4 | 1.0 | 0.32 | 0.25 | 1 |
| ULTRA | 3 | 8 | 1.0 | 0.40 | 0.25 | 1 |

- **エスカレーション起動**: HIGH で起動 → 最初の60フレームの平均が **10ms未満なら ULTRA へ昇格**、**22ms超なら BALANCED へ直行**(以後は降格済み扱い)、その間なら HIGH のまま。「最大で起動して降格」は初回のカクつきと弱GPUでのコンテキストロストを招くため採らない。バックグラウンドタブで計測窓が汚れた場合は窓を破棄する
- **AUTO降格**: 60フレーム窓の平均が18ms超を2窓連続で1段降格。**セッション中1回だけ再昇格可能**(20窓連続12ms未満)— 一度の不調で画質が固定される問題への対処
- **手動固定**: 右下チップで AUTO / ULTRA / HIGH / BALANCED を選択(自動制御は停止)
- `bloomScale` を単独で動かしてはならない(内部解像度を変えると同じ強度でも見た目が変わる)

---

## 5. 展示仕様

### 5.1 HOPF FIBRATION

S³ ⊂ C² において、ホップ写像 h(z₁,z₂) = (2Re(z₁z̄₂), 2Im(z₁z̄₂), |z₁|²−|z₂|²) の逆像(ファイバー)は大円になり、互いに絡み合う(ホップリンク)。

- 既定600本(上限1200)× 192セグメント
- 分布: 緯線リング(面積重み付き)/ 大円 / フィボナッチ
- 彩色: 経度φでIQコサインパレット、輝度は `0.2 + 0.8·sinθ`(赤道が最も明るく、極寄りも消えない)。ファイバー内では一定 —「1本の光の輪」として読めることを優先
- θ範囲は極を避けてクランプ(w→1 で投影が無限へ飛ぶため)

### 5.2 CLIFFORD TORUS

{(cos u, sin u, cos v, sin v)/√2} — S³ 上の**平坦**トーラス。S³ を二つの合同なソリッドトーラスに分ける境界。

- u円族・v円族(各既定36本 × 160セグメント)を色で区別(シアン系 / マゼンタ系)
- 歳差平面は **(0,3)**。(0,2) では w が |w|≤0.707 に固定され、**裏返りが原理的に起きない**(実装時の解析で判明)。(0,3) では γ=π/4(既定 t≈15.7秒)で w=1.0000 に到達し、投影半径が81まで膨張して自身を通り抜ける

### 5.3 POLYTOPE EXPLORER

- 族3種 × n=3〜10(ギャラリー既定はテッセラクト = cube n=4。クラス単体の既定値 n=10 は性能ストレス検証用で、`?exhibit=polytope` もギャラリー既定を使う)。最悪ケース(10次元超立方体)で1024頂点・5120辺・20,480線分
- 色は**隠れた軸の深度**(回転後の最終軸座標)— 4次元以上の奥行きを色として読ませる
- 密度補正: 加算合成の重なりは辺数に比例するため、基礎輝度に `1/edgeCount` の線形補正(無補正の10次元超立方体は画面が完全な白面になった)

### 5.4 PERSPECTIVE(視点次元セレクタ)

観測者次元 m ∈ {2,3,4} × 対象次元 n ∈ {2..6}。

| モード | 条件 | 内容 |
|---|---|---|
| 断面 | m < n | 対象が観測者超平面を通過する断面のモーフ + 神視点インセット |
| 影 | m < n | n→m の投影(直交/透視) |
| X線俯瞰 | m > n | 「上の次元からは内側がすべて見える」演出シーン |

- **神視点インセット** — メインレンダラーの scissor 第2パスで描画(第2のcanvas/WebGLRendererを作らない)。マッピングは inset(X,Y,Z) ← object(x₀, x_axis, x₁) で、超平面 {x_axis = s} が文字通り水平面 Y=s になる
- **掃引振幅は対象の厚みに追従** — 外接半径基準の固定振幅では断面が45%の時間で空になった(テッセラクトの座標は±1/2で、外接半径1に届くのは対角が揃った姿勢のみ)。姿勢ごとに実測して1.04倍を使い、断面が存在する時間比率 55%→83%
- X線シーン: (m=3,n=2) 平面世界の家・部屋・住人を斜め上から / (m=4,n=3) 3D の家をX線透過(壁も皮膚も隔てにならない)

### 5.5 物語シーン「extentsモーフ」

全章が**同一の6次元超立方体**(64頂点・192辺)。

```
extent[k] = clamp01(dimLevel − k)          // k = 0..5
input[v*6+k] = base6[v*6+k] * extent[k]    // 回転・投影の前にスケール
```

dimLevel=0 で点、1で線、3.5で立方体がテッセラクトへ半分押し出し中 — **連続的・可逆・スクラブ可能**な押し出しモーフになる。

- 回転スケジュール: (0,1)(0,2) は常時、(1,2) は低次元でのみ弱く回り4次元ゲートで消える(2次元の正方形が周期的に線に潰れるのを防ぐ)、**等傾二重回転 (0,3)+(1,2) は dimLevel 3 から重み付きで始動**(4次元到達の感情的な山場)、(2,4) は4から、(0,5) は5から
- 重みは積分位相で適用(重みだけを切ると位相が凍り、巻き戻し時に形が潰れる — 実装時に発見)
- 次元kに沿う辺は輝度 `extent[k]`。伸長中の軸はゴールドへブレンド(次元が生まれる瞬間の閃光)

---

## 6. スクロール機構

- **スクロールリスナーを一切張らない**。毎フレーム rAF 内で `window.scrollY` を読む(モバイルSafariのリサイズストーム耐性)
- セクション実測は `remeasure()` に集約(生成時 / debounce後のresize / `fonts.ready` / 初回フレーム)
- 章内 localT の**前半40%で次元を伸ばし、残り60%は読書のプラトー**
- `expSmooth`(rate 6)でフレームレート非依存の平滑化
- 物語→ギャラリーはスクロールハイジャックせず、エピローグのCTAボタンで明示遷移(scrollY保存・復元)

### 6.1 URL と履歴(`src/core/route.ts` — Phase 13)

モードと表示中の展示は **`?gallery=<exhibitId>`** に載る(物語 = パラメータ無し)。
これが無かった頃は「展示を人に送れない」だけでなく、**ギャラリー入場後の戻る /
iOS の端スワイプでサイトごと離脱していた**。

| 操作 | 履歴 |
|---|---|
| 物語 → ギャラリー | 物語エントリへ `replaceState({d:'narrative', y:scrollY})` してから `pushState({d:'gallery', exhibit, owned:true})` |
| タブ切替 | `replaceState` — **4展示で履歴を汚さない**(戻るは常に「入る前」へ帰る) |
| Esc / トップナビ「物語」 | `owned` なら `history.back()`、深リンクなら `replaceState` |
| popstate | URL を再パースして `Gallery.applyRoute()` へ。**Esc と同じ 1 本の階段**を降りる |

判断の根拠:

- **ハッシュは不可** — `#gallery` も `ch-prologue`…`ch-epilogue` も実在の id。フラグメント
  スクロールは**物語の状態そのものである scrollY** を壊し、ScrollDirector の毎フレーム読みと食い合う
- **パスは不可** — GitHub Pages に SPA フォールバックが無い。`/gallery/hopf` はリロードで
  404 になり、`/dimension/` のサブパスも落ちる
- **`?exhibit=` と重ねない(必須)** — `?exhibit=` は同じ4つの id を取る単独ブートで、
  `#narrative` / `#hud` / `#gallery` / `#mode-nav` を **DOM から削除する**。共有リンクが
  そちらへ落ちたら受け取った人はクローム無しの開発ハーネスに閉じ込められる。
  `main.ts` は `?exhibit=` を**先に**判定し、そのとき Router は構築しない
- URL は必ず `new URL(window.location.href)` から組む — サブパス配信・カスタムドメイン・
  他のクエリ・ハッシュが、この層が何も知らないまま保たれる
- **`owned` は履歴エントリ自身に焼く**。進む/戻るでエントリと一緒に旅するので往復しても
  判定がずれず、リロードも越える。深リンク(エントリ0)では `back()` を**呼ばない** ──
  呼べばサイトを離れる、まさに直そうとしているバグそのものになる
- **没入モードとドロワーは履歴に入れない** — `setUiHidden` はキャンバスの素のタップから
  到達するので、作品を見回すだけで履歴が溜まる
- **遷移中の popstate は 1 つだけ預かる**(`pendingRoute`)。`enterGallery`/`exitGallery` は
  `busy` で早期 return し void を返すので、popstate 側は却下を検知できない。`busy` は幕の
  40+480+40 = **560ms** 続き、iOS の端スワイプ連打はこれより速い。落とすと**履歴インデックスが
  恒久的にずれる**。10連打の実測で最終 URL と描画モードが一致することを確認済み
- **深リンク入場は幕を張らない** — `.tx` は z-index 80、`.pl` は 90 なので、幕はプリローダの
  **下**で動く。被せても 1.1 秒ぶん「止まったローダー」が伸びるだけになる
- **`history.scrollRestoration` は `'auto'` のまま**。`ScrollDirector` の初フレーム処理
  (リロード復帰時の 0 からの助走を防ぐ)がブラウザの自動復元の上に建っている。
  実測: ギャラリーでリロード → 戻る、でブラウザは 2080 を復元したが、フェード
  コールバック内(約250ms後)の `scrollTo(0, 6800)` が勝った。**競合しない**
- 物語エントリに焼く `y` は `savedScrollY` が救えない唯一の場合のため ── ギャラリーに
  居る状態でリロードすると新しい Gallery の `savedScrollY` は 0 になる(実測で確認)

---

## 7. UI/UX アーキテクチャ

### 7.1 コンポーネント基盤(`src/ui/components/`)

依存ライブラリなし。共通契約:

```ts
interface Component { readonly el: HTMLElement; destroy(): void; }
h(tag, className?, attrs?): HTMLElement   // 全コンポーネント共通のDOMビルダー
play(el, keyframes, options)              // Web Animations API の単一入口(reduced-motionガード内蔵)
```

| コンポーネント | 役割 |
|---|---|
| Preloader | 文字が1字ずつ立ち上がる入場。進捗は実際の準備状態(フォント→初回フレーム)に連動、最低表示1.1秒、上下に割れて退出 |
| Cursor | ドット+遅延追従リング。ホバー1.45倍、キャンバス上で「DRAG」表示。タッチ端末では非搭載 |
| SplitText | 行/文字のマスクリビール(aria保持) |
| MagneticButton | カーソルへの吸着(半径・強度指定、スプリングバック) |
| TransitionOverlay | 1pxグラデーションが先導する2層ワイプ |
| Tabs | 番号+EN+JPの3段、インジケータFLIPスライド、完全なtablistキーボード操作 |
| ExhibitHeader | 章題の文字リビール(入りは劇的に、出は静かに220ms) |
| Drawer | 解説ドロワー(role=dialog、段落の階段状リビール) |
| Hud | モード連動の読み取り値(物語: x.xx D / ギャラリー: EXHIBIT n/4) |
| controls/ | Slider(ドラッグ中の値バブル)/ Segmented(ピル滑走)/ Toggle / Readout / PanelButton / Panel |
| SoundToggle / ImmersiveToggle | 音響・没入モードのチップ |

- `createPanel(root, title)` ファサードを維持しており、**展示側のパネル構築コードは差分ゼロ**でコンポーネント化された
- 全コンポーネントが `prefers-reduced-motion` に対応(durations 0 / translate 無効)

### 7.2 デザイントークン

```css
--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
--ease-out-quint: cubic-bezier(0.22, 1, 0.36, 1);
--ease-inout-soft: cubic-bezier(0.65, 0, 0.35, 1);
--t-fast: 180ms; --t-med: 420ms; --t-slow: 800ms; --t-reveal: 1100ms;
--surface: rgba(10,12,24,0.55);  --hairline: rgba(255,255,255,0.08);
--s1..--s7: 4/8/16/24/40/64/104px;   /* 8px基準 */
```

配色: 背景 `#05060f` / シアン `#4fd8ff` / マゼンタ `#ff4fd8` / バイオレット `#9d6bff` / ゴールド `#ffd24f`

### 7.3 タイポグラフィ

@fontsource で自己ホスト(CDN依存なし、オフライン動作)。

- 英語ディスプレイ: **Unbounded** 600/800(letter-spacing .14em)。字数に応じて幅を満たすよう `clamp(2.2rem, calc(var(--display-fit)/(文字数×0.98)), 10rem)` で自動フィット
- 日本語本文: **Zen Kaku Gothic New** 400/500(line-height 2.1、unicode-rangeスライスで必要グリフのみ取得)
- 数値・HUD: **Space Mono**

### 7.4 モバイル方針(図が主役)

「UIが図を隠すなら本末転倒」を原則とする。

- ボトムシートは**初期状態でコラプス**(掴み手+見出しの帯のみ。高さは実測して `--sheet-h` に書き戻す。CSSのフォールバックは 3.5rem、実測例 63px)
- **没入モード** — キャンバスをタップ(タッチのみ。マウスはオービット操作と衝突するためアイチップ専用)で全UIがフェード。Esc の優先順位は **① 没入モードの復元 → ② ドロワーを閉じる → ③ ギャラリー退出**
- **セーフエリア補正** — `camera.setViewOffset` でシートやタブ分を避け、被写体が空き領域の中央に来る(実測111px上昇)
- **縦持ちドリー** — 縦画面ではFOVが縦基準で横視野が3.46倍狭くなるため、カメラを最大1.6倍引く(可視幅3.81→6.10ユニット)
- **全画面スクリムの禁止** — 文字可読性のための画面全体グラデーションはコントラストを59%奪う。テキストブロック直下の局所グラデーション(画面暗化は1/6)+2段シャドウで代替
- 横持ちの短い画面(高さ<600px)はデスクトップのサイドドック配置へフォールバック

---

## 8. 音響仕様(`src/audio/` — 完全プロシージャル、音声ファイルなし)

### 8.1 構成

```
カテゴリGain(ambient/sfx/bell) → masterGain → DynamicsCompressor → destination
```

- 既定 **OFF**。右下チップでオプトイン、選択は localStorage に永続化
- AudioContext はユーザー操作後にのみ生成(自動再生規制準拠)
- タブ非表示で suspend

### 8.2 音源

| 音 | 合成 |
|---|---|
| アンビエント | 55.00 + 55.35Hz(0.35Hzのうなり=呼吸)+ 110.7Hz、lowpass 220Hz / バンドパスノイズ(LFO 0.07Hz)/ 1244Hzのシマー |
| **次元の鐘** | 基音98Hzの第(k+1)倍音: **196 / 294 / 392 / 490 / 588 / 686Hz**。倍音 ×2.76・×5.40 の非調和成分でガラスの鐘に。**次元を跨ぐ瞬間に鳴る(上りも下りも)** |
| 操作音 | ホバー(37ms)/ クリック / タブのスウィープ / スライダーのティック / ギャラリー入場のグライド |

### 8.3 バイノーラル層(脳波周波数)

```
片側: Oscillator(sine, fc ∓ beat/2) → Gain(0.05) → StereoPanner(∓1) → fade → ambient
```

- **キャリア 196Hz** — 1次元の鐘と同一音。全6つの鐘と純正比(1:1, 3:2, 2:1, 5:2, 3:1, 7:2)を成す唯一の選択。55Hzのドローンはバイノーラルのキャリアには低すぎる
- **左右対称分割** — 中心音高を196Hzに固定し、各耳の変位は±5Hz以内
- **うなり周波数 = 次元の階段**:

```
beat = 2 + (4/3) × dimLevel      [Hz]
```

| 次元 | 0 | 1 | 2 | 3 | 4 | 5 | 6 / ギャラリー |
|---|---|---|---|---|---|---|---|
| Hz | 2.00 | 3.33 | 4.67 | **6.00** | 7.33 | 8.67 | **10.00** |
| 帯 | δ | δ | θ | θ | θ | α | α |

鐘が「1次元ごとに +98Hz」であるのと**同じ算術の階段**を、音高ではなく脈拍に敷く。私たちの3次元がちょうどθ波6.00Hz、ギャラリー(長時間の静止探索 = 実際に持続曝露が起きる唯一の場所)がα波10Hz固定。

- 遷移は `setTargetAtTime(τ=0.8s)` で滑らかに(スクロールの平滑化より遅らせ、音が像に追従する)
- **この層に振幅変調(LFO)を一切かけない** — かけるとモノラル手がかりが復活し、バイノーラルではなくアイソクロニックになる

### 8.4 文言規律(厳守)

効果効能を主張しない。記述するのは**刺激の事実のみ**。

- 禁止: リラックス効果 / 集中力が高まる / 脳波を整える / 瞑想状態に導く / ヒーリング / 〜に効く
- 可: 「θ波帯のうなり」「左右の耳にわずかに違う高さ」「毎秒六回」

初回有効化時に一度だけ表示: 「ヘッドフォンを ── うなりは空気ではなく、耳の中で生まれる。」
(バイノーラルは両耳分離が物理的に必須。スピーカーではモノラルのうなりに退化するが、それは許容する)

### 8.5 実測値(OfflineAudioContext、コンプレッサー通過後)

| 項目 | 値 |
|---|---|
| 左右の周波数差 | 目標値との誤差 **0.000Hz**(2/6/10Hz で検証) |
| チャンネル分離 | 109〜126dB(要求40dB) |
| 片耳の包絡線変動 | 1.005〜1.033(=平坦、真のバイノーラル) |
| アンビエント+バイノーラル | peak 0.113 / rms 0.050 |
| 全部同時(+鐘+クリック) | peak 0.185、クリッピングなし |

---

## 9. アクセシビリティ

- `prefers-reduced-motion`: 全アニメーション時間0・グレイン静止・自動回転停止
- 完全なフォーカスリング(基本 1pxシアン offset 3px、パネル内コントロールなど一部は 2px offset 2px)、`role=tablist/tab/tabpanel` のロービングtabindex、`role=dialog aria-modal` のドロワー(フォーカス復帰付き)
- 章テキストは実HTML(スクリーンリーダー・検索エンジン対応)。SplitTextは aria-label とspanの aria-hidden で原文を保持
- 音響は既定OFF・完全オプトイン。光過敏性は視覚由来のため非該当だが、アイソクロニック(強い振幅パルス)は**採用しない**方針

---

## 10. パフォーマンス

| 項目 | 実測 |
|---|---|
| CPUホットパス(10次元超立方体) | 1.74 ms/frame |
| Hopf 600ファイバー(11.5万線分) | GPUパスによりCPU実質ゼロ |
| バンドル(gzip) | three 130KB + アプリJS 67KB + CSS 132KB |
| 音響 | 音声アセット0(全てランタイム合成) |

CSSが大きいのは @fontsource の日本語フォント定義(121のunicode-rangeスライス)を含むため。実際に転送されるのは使用グリフを含むスライスのみ(各7〜15KB)で、単一ファイル版(966KB)を避けた結果。

---

## 11. 開発フロー

- **1フェーズ = 1ブランチ = 1 PR**(PR #1〜#15)
- **モデル分担**: 難所(数学コア・GPUシェーダー・スライス数学)と統合判断・全PRレビューは Fable、各フェーズ実装は Opus サブエージェント。**不具合修正時は独立監査サブエージェントを立て、実測に基づくプランを作ってから実装**
- **品質ゲート**(全PR必須): `npx vitest run` 全緑 + `npm run build` 成功 + ブラウザ実機確認 + コンソールエラーゼロ

```bash
npm install
npm run dev          # 開発サーバー(--host でLAN公開、スマホ実機確認用)
npx vitest run       # 数学コアのテスト(35件)
npm run build        # 静的ビルド → dist/
npm run preview      # 本番ビルドの確認(:4173)
```

URL パラメータ:

| パラメータ | 役割 |
|---|---|
| `?gallery=hopf\|clifford\|polytope\|perspective` | **公開仕様**。ギャラリーの指定展示へ直行する共有可能なリンク(§6.1) |
| `?exhibit=hopf\|clifford\|polytope\|perspective` | **デバッグ用**。物語もギャラリーシェルも迂回し、展示単体 + OrbitControls を起動する。シェルを DOM から削除するので**共有には使えない** |

両者は決して重ねない。`?exhibit=` が先に判定され、そのとき Router は構築されない。

DEVフック `window.__DIMENSION__` はブートパスによって形が異なる:

| 起動 | 内容 |
|---|---|
| 通常(物語) | `engine, narrative, scrollDirector, overlays, quality, router, gallery(初回入場まで null), grade(on), enterGallery(), exitGallery(), setScroll(y), renderOnce(steps), audio` |
| `?exhibit=` | `engine, exhibit, quality, grade(on), renderOnce(steps), audio` |

`enterGallery()` / `exitGallery()` は**履歴経由**なので、ヘッドレス検証も本番と同じ経路
(URL 更新・戻るの行き先)を通る。

`renderOnce(steps)` はブラウザペイン非表示で rAF が止まる環境でも合成フレームを進めて1枚描画するための検証用。

---

## 12. 既知の罠(実装時に踏み抜いた地雷)

番号はソース中のコメント(「既知の罠 #n」)と対応する。

1. **ステレオ投影の w→1 爆発** — 極を通るファイバーは無限遠へ飛ぶ。シェーダー内で長すぎる線分を縮退させる
2. **透視カスケードの退化** — 外接半径1の正規化が保証するのは第1段だけ。各段の倍率が累積し、9次元以上では dist=2.4 で分母の符号が反転する(実測: 10次元で投影半径178)。姿勢を走査して実効 dist を自動で伸ばす
3. **LineMaterial.resolution** — three の `LineSegments2.onBeforeRender` が毎フレーム上書きするため、`registerLineMaterial` の防御は実質無効(linewidth が CSS px 基準になるので害はない)
4. **three ヘルパの毎フレームアロケーション** — `setPositions()` / `setFromPoints()` / ループ内 `new Vector3` は禁止。`needsUpdate` は attribute ではなく **InterleavedBuffer** に立てる(attribute側では GPU へ反映されない)
5. **モバイル Safari のリサイズストーム** — アドレスバー伸縮で resize が連発する。debounce + `visualViewport` + `svh` 単位、スクロールリスナー不使用
6. **加算合成の白飛び** — 密度補正は辺数に**線形**(√ では足りない)。基礎輝度は ACES 前提で抑える
7. **タブ非表示後の dt スパイク** — `dt = min(dt, 1/20)`
8. **WebGL コンテキストロスト** — 日本語オーバーレイでリロードを案内する
9. **スライスの退化** — 超平面が頂点をちょうど通ると交差判定が不安定。オフセットに微小 eps を加える
10. **scissor の後始末** — インセット描画後に `setScissorTest(false)` とフルviewportへ復帰しないと次フレームの composer が壊れる
11. **onBeforeCompile の脆さ** — three のシェーダー文字列が変わると置換アンカーを失う。バージョンを exact で固定し、失敗時は CPU パスへフォールバック
12. **フルDPR + 高MSAA のメモリ帯域** — エスカレーション起動で弱GPUには到達させない。バックグラウンドタブで rAF が絞られた計測窓は破棄する(捨てないと弱端末と誤認して BALANCED に固定される)

番号の付いていない、しかし高くついた発見:

- **色空間リーク** — composer の RenderPass は GL のクリアカラーを使い、OutputPass 後に残った sRGB 値が次フレームの線形バッファへ流れ込む。クリアカラーは黒に固定し、背景色はネブラ球が線形値で担う
- **AudioParam.value はグライド中に古い値を返す**(Chrome)— 検証は実グラフのタップで行う
- **全画面スクリムは図の敵** — 文字可読性のためのビューポート全面グラデーションは線のコントラストを59%奪う(§7.4)

---

## 13. デプロイ

**GitHub Actions → GitHub Pages** で自動公開している(`.github/workflows/deploy.yml`)。

```
push to main
  → npm ci
  → npx vitest run       # 失敗すれば公開されない(品質ゲート)
  → npm run build
  → actions/upload-pages-artifact(dist/)
  → actions/deploy-pages
```

- Pages の公開元は API で `build_type=workflow` に設定済み(リポジトリ設定のブランチ指定ではなく Actions 経由)
- `vite.config.ts` の `base: './'`(相対パス出力)により、`https://ops324.github.io/dimension/` のようなサブパス配信でも変更なしで動く。カスタムドメインに切り替えても同様
- 手動再実行は `workflow_dispatch` でも可能

---

## 14. 今後の候補

- 展示ごとのカメラホーム微調整(縦持ちドリーは現在4展示共通の1.6倍)
- 実機DPR3端末での品質ティア再確認
- 4次元回転の直接操作(ドラッグで回転平面を選ぶ)
- カスタムドメインの検討
